/**
 * Playlist feature — app-wide constants
 * Convention: UPPER_SNAKE_CASE
 */

/**
 * Artificial delay before mock data resolves.
 * 800ms gives the skeleton enough time to be clearly visible to the user.
 */
export const PLAYLIST_MOCK_DELAY_MS = 800;

export const PLAYLIST_FALLBACK_COVER = "/covers/song1.jpg";

/** Sentinel ID used when no real playlist ID is present in mock mode */
export const PLAYLIST_MOCK_DEFAULT_ID = "123";
